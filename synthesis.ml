module Sequence = Base.Sequence
type upProp = (Id.t * Hfl.baseSort) list * Hfl.clause list (* \exists.x.\phi(x,r) *)

(* let choose_head ep penv sort spec = () *)
  
  
(* let rec gen_e ep penv sort spec dmax =  *)
(*   let head_candi = choose_head ep penv sort spec in *)
    

